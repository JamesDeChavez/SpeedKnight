import './styles.css'

const Profile = () => {
    const className = 'Profile'
    return (
        <div className={className}>
            <h2 className={`${className}__title`}>Profile</h2>
        </div>
    )
}

export default Profile